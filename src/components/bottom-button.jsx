import { Button } from 'primereact/button'

export const BottomButton = props => <Button style={style} {...props} />

const style = {
  backgroundColor: '#74BD52',
  display: 'flex',
  margin: 'auto',
  border: 'none',
}
